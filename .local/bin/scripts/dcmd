#!/bin/sh

cmds=(
  "btmirecon"
  "btmidis"
)

ch=$(printf '%s\n' "${cmds[@]}" | dmenu -p "cmd")

case $ch in
  btmirecon) bt mi r ;;
  btmidis) bt mi d ;;
  *) notify-send "unknown dcmd: $ch"; exit 1 ;;
esac
