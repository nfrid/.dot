#!/bin/sh

host=nf@192.168.1.141:/
port=2222
mnt=$HOME/TEMP/Music
mus=$HOME/Music/*

mkdir -p $mnt
sshfs -o HostKeyAlgorithms=ssh-rsa -p $port $host $mnt || exit 1

rsync -crvvuP --delete --checksum $mus $mnt

fusermount -u $mnt
rmdir $mnt
