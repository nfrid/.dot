#!/bin/bash

[[ $(bspc query -D --names -d focused) == "e" ]] &&
  target=last ||
  target=e

[[ $1 == "-m" ]] &&
  id=$(bspc query -N -n) &&
  bspc node -d $target &&
  bspc node -f ${id} ||
  bspc desktop -f $target
